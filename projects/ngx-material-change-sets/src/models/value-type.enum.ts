/**
 * Different Value Types used to determine how a change value should be pretty printed.
 */
export enum ValueType {
    ARRAY = 'array',
    OBJECT = 'object',
    DATE = 'date',
    OTHER = 'other'
}